
spring:
  datasource:
#    password: L3e5F7u8
#    username: root
    password: root
    username: root
#    url: jdbc:mysql://111.231.205.128:3306/music_fire?useSSL=false&characterEncoding=utf8
    url: jdbc:mysql://localhost:3306/music_fire?useSSL=false&characterEncoding=utf8
    driver-class-name: com.mysql.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 20
  redis:
    host: redis2.public
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8



#mybatis
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.musicfire.modular.system.entity
  global-config:
    #主键类型  0:"数据库ID自增", 1:"用户输入ID",2:"全局唯一ID (数字类型唯一ID)", 3:"全局唯一ID UUID";
    id-type: 0
    #字段策略 0:"忽略判断",1:"非 NULL 判断"),2:"非空判断"
    field-strategy: 2
    #驼峰下划线转换
    db-column-underline: true
    #刷新mapper 调试神器
    refresh-mapper: true
    #逻辑删除配置
    logic-delete-value: 0
    logic-not-delete-value: 1

init:
  appRole: false #初始化应用角色信息（角色中心）
  appInfo: false #初始化应用资源信息（应用中心）
  adminRole: false #初始化admin账号角色信息（角色中心）
server:
  port: 80
